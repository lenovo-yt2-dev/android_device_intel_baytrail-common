# Additional platform_app rules
allow platform_app surfaceflinger_tmpfs:file { read write };

allow platform_app unlabeled:dir { read getattr open setattr };
allow platform_app unlabeled:file { read write getattr open };
allow platform_app graphics_device:dir search;
